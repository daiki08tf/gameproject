/* Monster Ranch 1.6 — discovery, lineage and awakening */
import { state } from '../state.js';import { MUTATION_DEFS,mutationChance,mutationsForSpecies } from '../data/monsterMutations.js';
function ensure(){state.data.ranchMutations||={discovered:{}};state.data.ranchMutations.discovered||={};}ensure();function boardComplete(speciesId){const nodes=state.ranchSpeciesBoardNodes||[],ranks=state.ranchBoardRanks?.(speciesId)||{};return nodes.length>0&&nodes.every(n=>(ranks[n.id]||0)>=n.maxRank);}function discovered(id){ensure();return!!state.data.ranchMutations.discovered[id];}function mark(id){ensure();state.data.ranchMutations.discovered[id]=true;}function applyMutation(inst,def){inst.mutationId=def.id;inst.mutationName=def.name;inst.mutationTier=def.tier;inst.mutationTrait=def.trait;inst.mutationStatMult={...(def.statMult||{})};mark(def.id);return inst;}
state.ranchMutationDef=id=>MUTATION_DEFS[id]||null;state.ranchMutationDiscovered=discovered;state.ranchMutationLineage=function(speciesId){return mutationsForSpecies(speciesId).map(x=>({...x,discovered:discovered(x.id)}));};
state.rollRanchMutation=function(instanceId,{beastDen=false}={}){const inst=this.data.companionInstances?.[instanceId];if(!inst||inst.mutationId)return null;const research=this.ranchResearch?.(inst.speciesId)||{recruited:0};for(const def of mutationsForSpecies(inst.speciesId).filter(x=>x.tier==='rare')){const chance=mutationChance({tier:def.tier,recruited:research.recruited,boardComplete:boardComplete(inst.speciesId),beastDen});if(Math.random()<chance){applyMutation(inst,def);this.save();return{...def,chance};}}return null;};
state.canAwakenRanchMutation=function(instanceId,mutationId){const inst=this.data.companionInstances?.[instanceId],def=MUTATION_DEFS[mutationId];if(!inst||!def||def.baseSpeciesId!==inst.speciesId)return{ok:false,reason:'invalid'};if(def.requiresMutation&&inst.mutationId!==def.requiresMutation)return{ok:false,reason:'lineage'};const bond=Number(inst.bondLevel)||1;if(def.tier==='upper'&&bond<8)return{ok:false,reason:'bond',required:8};if(def.tier==='mythic'){if(bond<10)return{ok:false,reason:'bond',required:10};if((this.ranchResearch?.(inst.speciesId)?.recruited||0)<100)return{ok:false,reason:'mastery',required:100};if(!boardComplete(inst.speciesId))return{ok:false,reason:'board'};}if(def.id==='fenrir'&&(!discovered('forest_white_wolf')||!discovered('forest_black_wolf')))return{ok:false,reason:'lineageKnowledge'};if(def.id==='ouroboros_slime'&&(!discovered('metal_slime')||!discovered('blood_slime')||!discovered('magic_slime')))return{ok:false,reason:'lineageKnowledge'};return{ok:true};};
state.awakenRanchMutation=function(instanceId,mutationId){const check=this.canAwakenRanchMutation(instanceId,mutationId);if(!check.ok)return check;const inst=this.data.companionInstances[instanceId],def=MUTATION_DEFS[mutationId];applyMutation(inst,def);this.save();return{ok:true,mutation:def};};state.ranchMutationStatMult=function(instanceId){return this.data.companionInstances?.[instanceId]?.mutationStatMult||{};};
