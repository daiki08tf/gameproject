import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const data=fs.readFileSync('js/data/settlementCapital.js','utf8'),runtime=fs.readFileSync('js/patches/settlementCapital.js','utf8'),ui=fs.readFileSync('js/patches/settlementCapitalUi.js','utf8'),home=fs.readFileSync('js/patches/homeNavigation.js','utf8');
test('S18 unlocks only at Hall 20 and never extends facility levels',()=>{assert.match(data,/FRONTIER_CAPITAL_MIN_HALL = 20/);assert.doesNotMatch(runtime,/hall\s*\+\+|hall\s*=\s*21|maxLevel\s*:\s*2[1-9]/);assert.match(runtime,/Lv20以降は施設Lvを増やさず/);});
test('capital uses three horizontal projects and one existing-battle crisis hook',()=>{for(const id of ['ancientRelay','finalArchive','capitalBulwark','capitalConvergence'])assert.match(data,new RegExp(id));assert.match(runtime,/pendingEncounter/);assert.match(runtime,/noAutoReward:true/);assert.doesNotMatch(runtime,/BattleEngine|new Battle|autoWin/);});
test('capital reuses existing progress without new currency or timers',()=>{for(const token of ['settlementEndgameSummary','settlementResearchSummary','settlementDefenseSummary','abyssBestDepth','activeWorldTier'])assert.match(runtime,new RegExp(token));assert.doesNotMatch(runtime,/setInterval|setTimeout|Date\(|currency|capitalCoin|prestigePoint/);});
test('capital state stays under settlement3 and UI stays inside Settlement',()=>{assert.match(runtime,/root\.capital/);assert.doesNotMatch(runtime,/state\.data\.settlementCapital\s*=/);assert.match(ui,/#settlementContent/);assert.doesNotMatch(ui,/goSettlementCapitalBtn/);assert.match(home,/settlementCapital\.js/);assert.match(home,/settlementCapitalUi\.js/);});
